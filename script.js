// Imagine a chess board whose squares are marked as coordinates with the 8x8 grid of the board being numbered from 0-7. The top left corner space is 0, 0, and the bottom right corner space is 7, 7. Given the coordinates of a king on an otherwise empty chessboard, return an array of the spaces that king can move to. For example, chessKingMoves(0, 1) should return [[0, 0], [0, 2], [1, 0], [1, 1], [1, 2]].
// https://en.wikipedia.org/wiki/King_(chess)#Placement_and_movement

const chessKingMoves = (x, y) => {}

const chessRookMoves = (x, y) => {}

const chessBishopMoves = (x, y) => {}